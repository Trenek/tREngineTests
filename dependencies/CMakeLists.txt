add_subdirectory(tREngine EXCLUDE_FROM_ALL)

set(INCLUDE_DIRECTORIES)

set(LINK_LIBRARIES
    tREngine
)

set(INCLUDE_DIRECTORIES ${INCLUDE_DIRECTORIES} PARENT_SCOPE)
set(LINK_LIBRARIES ${LINK_LIBRARIES} PARENT_SCOPE)
